#include <stdio.h>//print y scan
#include <math.h>//pow, sqrt
#include <stdlib.h>//Apuntadores
#include <string.h>//Manejo de cadenas
#define MAX 100

int main(){
    
    return 0;
}

/*
char momo[MAX+1]="Hola, por favor no me repruebe";
    FILE* hola;
    hola=fopen("salida.txt", "a");
    fprintf(hola, "%s\n", momo);
    fclose(hola);
*/